projection;
strict ( 1 );
use draft;

define behavior for zgy_c_rap alias Professor
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action setActive;

}